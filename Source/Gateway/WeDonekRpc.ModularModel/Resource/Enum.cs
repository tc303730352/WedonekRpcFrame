namespace WeDonekRpc.ModularModel.Resource
{
        public enum ResourceType
        {
                API接口 = 2,
                RPC接口 = 4,
                RPC节点 = 8
        }
        public enum ResourceState
        {
                正常=1,
                失效=2,
        }
}
